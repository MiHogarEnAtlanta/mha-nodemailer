# Variables requeridas por la funcion de envio (Microsoft Graph).
# En LOCAL: copia este archivo a .env y rellena los valores.
# En NETLIFY: configuralas en Site configuration -> Environment variables (NO subas el .env real).

CLIENT_ID=
TENANT_ID=
CLIENT_SECRET=
SENDER_EMAIL=
RECIPIENT_EMAIL=
